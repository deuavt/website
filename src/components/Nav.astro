<nav>
	<div class="logo">
		<a href="/">CONNOR FRISBY</a>
	</div>
	<div class="links">
		<a href="/">HOME</a>
		<a href="/about">ABOUT ME</a>
		<a href="/tutoring">TUTORING</a>
		<a href="/contact">CONTACT</a>
	</div>
</nav>

<style>
	.logo a {
		font-size: clamp(1rem, 3vh, 3rem);
		font-weight: 700;
		white-space: nowrap;
	}
	.links a {
		font-size: clamp(0.75rem, 2.25vh, 2rem);
	}
	nav {
		display: flex;
		justify-content: center;
		flex-wrap: wrap;
		column-gap: 6vw;
		padding: 0 12%;
		align-items: center;
		position: sticky;
		width: 100%;
		z-index: 10;
		top: 0;
		color: rgba(0,0,0,0.7);
		background: rgb(255, 255, 255);
		box-shadow: 0px 2px 6px rgba(0,0,0,0.2);
		box-sizing: border-box;
	}
	nav a {
		display: inline-block;
		color: inherit;
		text-decoration: none;
	}
	.links {
		display : flex;
		flex-wrap: wrap;
		justify-content: center;
		gap: 6vw;
	}
	.logo {
		letter-spacing: 0.3em;
		color: rgba(0,0,0,0.7);
	}
	.logo a, .links {
		padding: 2.5vh;
	} 
</style>